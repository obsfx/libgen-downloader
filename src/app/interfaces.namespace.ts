import { List } from '../ui'

export namespace Interfaces {
    export interface Entry {
    }

    export interface EntryData {
        md5: string;
        title: string;
        author: string;
        extension: string;
    }
}
